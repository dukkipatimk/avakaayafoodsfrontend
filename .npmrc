# Do not create node_modules/.bin symlinks. The Hostinger build server reuses
# node_modules between deploys and its stale .bin symlinks cause npm install to
# fail with ELOOP. We never rely on .bin — scripts call tools by direct path —
# so disabling bin-links removes the failure entirely.
bin-links=false
